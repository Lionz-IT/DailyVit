{
  "name": "dailyvit-monorepo",
  "version": "1.0.0",
  "description": "DailyVit Project Monorepo",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix frontend && npm install --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev": "npx concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "start": "npx concurrently \"npm run start --prefix backend\" \"npm run preview --prefix frontend\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
